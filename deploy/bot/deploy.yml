---
- name: Deploy application
  hosts: bots
  vars_files:
    - shared.yml
    - secrets.yml
  tasks:
    - name: Auth Dockerhub
      ansible.builtin.include_tasks: deploy-auth.yml

    - name: Stop prev
      ansible.builtin.include_tasks: deploy-stop-prev.yml
    
    - name: Run Deploy
      ansible.builtin.include_tasks: deploy-run-new.yml
