---
- name: Deploy application
  hosts: all
  vars_files:
    - shared.yml
    - secrets.yml
  tasks:
    - name: Auth Dockerhub
      ansible.builtin.include_tasks: deploy-auth.yml
    - name: Run Deploy
      ansible.builtin.include_tasks: deploy-run-deploy.yml
