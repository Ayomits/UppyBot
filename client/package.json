{
  "name": "fear-monorepo",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo build --concurrency=4",
    "dev": "turbo dev --concurrency=4",
    "prod": "turbo prod --concurrency=4 --filter @fear/helper-bot",
    "format:fix": "turbo format:fix --concurrency=4",
    "lint:fix": "turbo lint:fix --concurrency=4",
    "format:check": "turbo format:check --concurrency=4",
    "lint:check": "turbo lint:check --concurrency=4",
    "test:watch": "vitest",
    "test:ci": "vitest --run",
    "prepare": "husky"
  },
  "keywords": [],
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "3.2.4",
    "husky": "^9.1.7",
    "turbo": "^2.5.6",
    "vitest": "^3.2.4"
  }
}
